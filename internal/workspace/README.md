# workspace

本目录负责确定一次应用运行使用的 Workspace 根目录。

## 解析规则

- 显式提供 `--cwd` 时，严格使用校验和规范化后的目标目录。
- 未指定时，优先探测当前目录所属的 Git 根目录。
- Git 探测失败时，安全回退到当前目录。
- 返回路径会进行绝对化、清理和符号链接解析。

本包不调用 `os.Chdir`，最终 Workspace 由 `internal/app` 显式传递给下游组件。
