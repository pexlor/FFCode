# 系统优化
1. 工具调用被权限系统拦截也要把信息返回给模型 done
2. 工具注册有问题，模型对话表示没有一些工具（工具被拦截了） done
3. 支持用户选择权限级别（P1）
4. 支持显示对话状态（思考？工具调用？）done
5. ctrl + c 打断对话 / 非模型输出状态退出命令行 done
6. ui 变好看一点（开头 + 对话框优化）done
7. 命令行输入的时候，输入法位置有问题 done
8. 支持多系统架构（长期）
9. / 选择带参数的命令时不要直接执行 比如（/new xxx）done
10. 持久化session优化
11. 完善发布入口（--version --help）done
12. 输出完整的工具执行命令(取消)
13. 优化系统输入
14. ui 适配 markdown done
15. 开启think功能/内置命令支持开关 done
16. 按上下键，可以跳出历史输入 done
17. git 目录探测（找出完整项目根目录）done
18. tool 的并发调用不在由 agent 控制，下沉到toolManager（控制粒度变细）
19. 整体调用链： agent -> 写入 Session -> context 构造 -> llm -> session -> tool -> session
    context 包含 Session ,当 Session 修改时更新 context 视图
    agent应该完全不知道 Session 的存在只知道 context ，避免每次都要重建 context
    context 也要持久化（利用Session id）,避免重启时还需要调用llm重建 context
20. 提炼出agent-core，和ui分开（不在同一目录下）
21. 系统提示词上移

# 模块功能实现
1. 长期记忆
2. subagent
3. agent team
4. skill 系统
5. hook 系统

# 长期任务
1. 架构优化（人类架构）
2. 权限系统优化（优化拦截策略）（更细粒度的拦截）
3. 上下文优化（最大化利用模型缓存）
4. 完善模块设计文档
5. 完善测试系统
6. 支持多系统架构
7. ui 优化
8. 日志系统

# bug
1. 权限bug 已修复
● 执行失败: active path is outside workspace
  输入已取消
› 你好

● 执行失败: active path is outside workspace

2. Anthropic 工具结果 ID 丢失导致后续请求持续返回 400（已修复）
   - 现象：远程命令等待密码等交互输入时中断当前 Turn，后续请求持续返回 `tool result's tool id() not found (2013)`。
   - 根因：并发工具执行等待期间收到 context cancellation 时，`executeTools` 返回了尚未填充的预分配结果，产生空 `ToolUseID` 并污染会话历史。
   - 修复：预先为每个结果保留原始 `ToolUseID` 和取消错误，已完成的工具结果再覆盖对应位置。
   - 回归：增加工具执行取消测试，并通过 `go test ./...` 和 `go test -race ./internal/agent`。

# Agent 核心能力优化

以下问题来自复杂代码任务中的实际运行表现。`benchmark/swebench-lite-20260726/agent-problem-analysis-50-cases.md` 仅作为问题证据和回归数据，不作为 Agent 的专用设计目标。

## P0：结构化运行协议（已完成）

1. 为 AgentEvent 提供稳定的机器可读输出协议，终端 UI、自动化调用方和评测工具都消费同一套结构化事件。
2. 不再通过终端正文中的 `done:`、错误文案或 ANSI 渲染结果推断 Agent 状态。
3. 统一停止原因：`end_turn`、`max_tokens`、`cancelled`、`deadline_exceeded`、`provider_error`、`tool_error`、`agent_error`。
4. 只有正常停止原因可以产生成功的 DoneEvent；输出截断、取消和超时必须使用独立的未完成事件。
5. 将运行状态、代码产物状态和验证结果分开建模，避免“Agent 停止运行”等价于“任务正确完成”。

验收标准：正文内容不会触发状态变化，不同调用方观察到的终止原因一致且可测试。

## P0：任务预算与收敛控制

1. 为单次 Agent Run 增加可配置的时间、工具循环和 token 预算，由交互式或自动化调用方按场景传入。
2. 将复杂任务划分为探索、实现、验证和收尾阶段，并根据剩余预算动态调整系统指令。
3. 探索阶段消耗过多但仍无代码变化时，要求模型选择当前最佳方案并开始实现。
4. 进入收尾阶段后停止扩大源码浏览和测试范围，只保留当前成果、运行最小验证并输出结论。
5. 已形成有效修改且相关验证通过后默认结束，只有明确未满足的需求才继续工作。
6. 降低默认最大工具循环次数，wall-clock 时间作为最终保护，避免 Agent 无限制运行。

验收标准：长任务能够在预算内稳定结束；已有有效修改时优先交付，不因重复分析丢失成果。

## P0：循环与进度检测

1. 记录工具名、规范化参数哈希、结果哈希、工作区变化和最近一次验证结果。
2. 相同工具调用且结果不变时，第 2 次给出收敛提醒，第 3 次阻止重复路径并要求重新决策。
3. 连续多个工具循环没有新证据、代码变化或决策推进时，触发实现或收尾提示。
4. 检测重复读取相同文件区间、重复执行相同命令、重复运行已通过测试和无变化的 diff。
5. 循环检测事件需要进入 transcript 和运行日志，便于后续分析策略是否有效。

## P1：停止原因与输出截断恢复

1. Agent 在没有 tool call 时仍必须检查 stop reason，不能无条件发送 DoneEvent。
2. 将 Anthropic `max_tokens`、OpenAI `length` 等 Provider 状态归一化为内部停止原因。
3. 遇到输出截断时最多进行有限次数的受控续写，复用现有上下文并要求继续当前决策，禁止重新开始分析。
4. 续写仍失败时发送 IncompleteEvent，保留已有回复、代码修改、使用量和停止原因。
5. 统一 `model.max_tokens` 与 `context.output_reserve` 的默认值和配置校验。

## P1：Provider 错误恢复

1. 定义结构化 ProviderError，包含 provider、HTTP 状态码、Retry-After、错误类型和是否可重试。
2. 对 HTTP 429、529、可恢复 5xx、连接重置和读取超时执行指数退避加抖动重试。
3. 对认证失败、不可恢复 4xx 和 context cancellation 不执行重试。
4. 将畸形工具 JSON 纳入统一重试策略，记录每次重试及最终失败原因。
5. 同时限制单次请求和整个 Agent Run 的重试预算，避免错误恢复耗尽任务预算。

验收标准：瞬时 Provider 错误可以透明恢复；无法恢复时准确返回 `provider_error`，不污染正常完成统计。

## P1：长任务上下文管理

1. 支持按工具循环 iteration 压缩当前未完成 Turn，而不仅压缩已经结束的历史 Turn。
2. 始终成对保留 tool call 和 tool result，保留最近若干完整工具循环。
3. 将较早轨迹压缩为当前目标、已确认事实、方案决策、已修改文件、验证结果、失败路径和下一步。
4. 在构建 ContextView 时传入正确的 CurrentTurnID，确保当前 Turn 的保留和淘汰策略按设计生效。
5. 使用 StoredMessage.Iteration 建立可审计检查点，控制长任务的累计输入、延迟和模型成本。
6. 摘要必须保留未完成事项和当前决策，避免压缩后重新探索已解决的问题。

## P1：工具调度安全性

1. 仅并发执行确认无副作用且互相独立的读取工具。
2. EditFile、WriteFile 和可能修改工作区的 Bash 调用必须串行执行。
3. 保持具有依赖关系的工具调用顺序，避免写入竞态和使用过期读取结果。
4. 为同文件并发写、工具取消、工具超时和部分结果返回增加 `go test -race` 覆盖。

## P1：运行检查点与恢复

1. Agent 被取消、超时或 Provider 中断时，保存 transcript、上下文摘要、代码变化、最近验证结果和最后决策。
2. 支持从已有会话和工作区状态继续运行，不强制从用户原始请求重新分析。
3. 恢复后的第一轮先核对现状和未完成事项，再决定继续实现、验证或直接收尾。
4. 检查点设置保留期限和清理策略，避免长期占用磁盘或注入过期状态。

## P2：结果质量门禁

1. 在预算允许时执行一次有上限的最终审查，核对用户需求、修改范围、回归风险和验证证据。
2. 最终审查只处理明确缺陷，不重新展开无边界探索。
3. 最终事件包含修改摘要、验证结果、未验证项和已知风险，供 UI 与自动化调用方统一展示。
4. 区分“没有必要修改”“无法修改”“修改未验证”和“修改已验证”，避免仅以非空 diff 判断成功。

## P2：Agent 回归评估

1. 建立覆盖正常完成、输出截断、Provider 错误、工具失败、循环、取消、超时和恢复的确定性测试集。
2. 使用 SWE-bench 等外部数据集验证复杂代码修复能力，同时增加真实交互任务和非 Git 工作区场景。
3. 对运行协议、错误重试、收敛控制、循环检测和上下文压缩分别进行消融测试。
4. 统一报告任务成功率、严格完成率、有效产物率、超时率、恢复成功率、平均工具循环和 token 成本。
5. 所有前后对比固定任务、模型、预算和环境，避免把评测噪声误认为 Agent 能力变化。
