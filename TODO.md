# 系统优化
1. 工具调用被权限系统拦截也要把信息返回给模型 done
2. 工具注册有问题，模型对话表示没有一些工具（工具被拦截了） done
3. 支持用户选择权限级别（P1）
4. 支持显示对话状态（思考？工具调用？）done
5. ctrl + c 打断对话 / 非模型输出状态退出命令行 done
6. ui 变好看一点（开头 + 对话框优化）done
7. 命令行输入的时候，输入法位置有问题 done
8. 支持多系统架构（长期）
9. / 选择带参数的命令时不要直接执行 比如（/new xxx）done
10. 持久化session优化
11. 完善发布入口（--version --help）done
12. 输出完整的工具执行命令(取消)
13. 优化系统输入
14. ui 适配 markdown done
15. 开启think功能/内置命令支持开关 done
16. 按上下键，可以跳出历史输入 done
17. git 目录探测（找出完整项目根目录）done
18. tool 的并发调用不在由 agent 控制，下沉到toolManager（控制粒度变细）
19. 整体调用链： agent -> 写入 Session -> context 构造 -> llm -> session -> tool -> session
    context 包含 Session ,当 Session 修改时更新 context 视图
    agent应该完全不知道 Session 的存在只知道 context ，避免每次都要重建 context
    context 也要持久化（利用Session id）,避免重启时还需要调用llm重建 context
20. 提炼出agent-core，和ui分开（不在同一目录下）
21. 系统提示词上移
22. token估算优化（利用每次调用llm api返回的token + 每次新增文本的 toekn 进行较为精确的估算）
23. 压缩窗口优化（保留系统提示词、早期关键上下文、最新上下文，把中间历史压缩）

# 模块功能实现
1. 长期记忆 done
2. subagent done
3. agent team (暂时不做)
4. skill 系统 done
5. hook 系统 done

# 长期任务
1. 架构优化（人类架构）
2. 权限系统优化（优化拦截策略）（更细粒度的拦截）
3. 上下文优化（最大化利用模型缓存）
4. 完善模块设计文档
5. 完善测试系统
6. 支持多系统架构
7. ui 优化
8. 日志系统

# bug
1. 权限bug 已修复
● 执行失败: active path is outside workspace
  输入已取消
› 你好

● 执行失败: active path is outside workspace

2. Anthropic 工具结果 ID 丢失导致后续请求持续返回 400（已修复）
   - 现象：远程命令等待密码等交互输入时中断当前 Turn，后续请求持续返回 `tool result's tool id() not found (2013)`。
   - 根因：并发工具执行等待期间收到 context cancellation 时，`executeTools` 返回了尚未填充的预分配结果，产生空 `ToolUseID` 并污染会话历史。
   - 修复：预先为每个结果保留原始 `ToolUseID` 和取消错误，已完成的工具结果再覆盖对应位置。
   - 回归：增加工具执行取消测试，并通过 `go test ./...` 和 `go test -race ./internal/agent`。

# Agent 核心能力优化
## 运行时可靠性
1. run 级时间、token预算、工具调用限制
目前只用MaxIterations来限制agent防止无限循环
无法控制时间、成本和工具数量
要区分三类 Token：
  ContextWindow：单次请求最多能放多少上下文。
  MaxOutputTokens：单次模型响应最多生成多少。
  RunTokenBudget：整个任务多轮请求累计最多消耗多少。
每次模型请求和工具调用前都进行预检查。预算不足时，不应直接报错，而应先强制进入“收尾阶段”；只有连最小收尾请求都无法执行时，才产生 incomplete/budget_exhausted。
时间预算建议使用软硬两级：
  软截止：例如总时间使用 75% 后进入收尾阶段。
  硬截止：通过 context.WithDeadline 取消模型和工具。
  保留窗口：预留一部分时间保存检查点和输出最终状态。
预算必须由交互 CLI、JSONL Runner、未来 Subagent 分别传入，而不是写死在 Agent 中。子 Agent 的预算必须是父 Agent 预算的子集。

2. 循环与无进展检测
循环检测不是简单判断“同一个工具调用了几次”，而是判断是否获得了新证据或推进了任务状态。
监测四类循环：
相同工具、相同参数、相同结果，例如反复读取同一文件区间。
相同命令、相同退出码和输出，例如重复运行已经通过的测试。
多轮没有工作区变化、没有新文件、没有新的测试结果。
在两个方案或两个文件之间来回切换，没有形成决策。
采用分级响应：
第 2 次重复：向模型注入提醒，指出该路径没有新信息。
第 3 次重复：拒绝同样的调用，要求选择新方案或进入实现。
连续若干轮无进展：强制切换阶段。
已有有效修改且验证通过：直接进入收尾。
没有修改且无法推进：以 incomplete/no_progress 结束。

3. Provider 429、529、5xx 重试
应重试：429、529、部分 500/502/503/504、连接重置、临时 DNS 错误、流建立前超时。
不应重试：401、403、无效参数、模型不存在、上下文超限、用户取消。
谨慎重试：流响应中途断开，需要确认本轮内容尚未提交。
关键规则是“先缓冲、后提交”。一次模型响应中的文本、Thinking 和 Tool Call 应先属于临时 attempt；只有请求正常结束后，才整体提交到 Session。否则流中途失败再重试，可能产生重复文本或重复工具调用。

4. 工具读并发、写串行
当前 Agent 会并发执行同一轮全部工具调用。这对多个读取操作有效，但并发写可能造成覆盖、读取旧状态或测试与编辑同时运行。
ReadFile/Grep/Glob：Read，可并发。
EditFile/WriteFile/ApplyPatch：Write，默认串行。
Bash：默认 Exclusive，因为命令是否修改文件通常难以准确判断。
明确识别为只读的 Bash 命令以后可以降级为 Read，但不是第一版必需。
调度规则：
连续且互相独立的读取工具并发执行。
结果仍按照模型原始调用顺序写回 Session。
遇到写工具时，等待之前的读取完成，然后串行执行。
写之后的读取必须看到新状态，不能与写并发。
两个目标路径明确不同的写操作，第一版仍建议串行；等 race 测试充分后再优化。
一个工具失败不应自动取消其他独立读取，但权限拒绝、用户取消和硬截止应传播取消。
这层逻辑应该放在 tool.Scheduler/Executor，Agent 只提交调用批次并等待结果，为未来 Subagent 共用。

5. 取消、超时检查点与恢复
Session 持久化记录“发生过什么”，检查点记录“任务做到哪里、接下来做什么”。二者不能混为一谈。

# 场景问题
## 上下文问题
1. 长对话继续写代码，Agent 还没调用模型就先压缩
2. 模型调用很多工具，成功后根据估算 tokens 决定压缩
3. API 报 context overflow
4. 压缩多次会降低质量

## 工具调用问题
